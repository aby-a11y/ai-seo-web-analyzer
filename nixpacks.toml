[phases.setup]
nixPkgs = ["chromium", "nss", "cups", "libdrm", "libxkbcommon", "glib", "atk", "gtk3", "pango", "cairo"]

[phases.install]
cmds = [
  "pip install -r backend/requirements.txt",
  "pip install playwright==1.40.0 pillow textstat nltk",
  "python -m playwright install chromium"
]

[start]
cmd = "cd backend && uvicorn server:app --host 0.0.0.0 --port $PORT"
